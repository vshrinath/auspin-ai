---
import BaseLayout from './BaseLayout.astro';
import HeaderSimple from "@salient/ui/test_components/Header/HeaderSimple";
const { frontmatter } = Astro.props;
---
<BaseLayout title={frontmatter.title} description={frontmatter.description}>
  <header class="bg-white">
    <HeaderSimple 
      eyebrow="Thinking" 
      title={frontmatter.title} 
      description={frontmatter.description}
      className="py-16 sm:py-24"
    />
  </header>
  <article class="max-w-7xl mx-auto px-6 pb-24 lg:px-8">
    <div class="mx-auto max-w-3xl">
      <div class="prose prose-indigo lg:prose-xl mb-16">
        <slot />
      </div>
      <footer class="mt-16 pt-8 border-t border-gray-100">
        <a href="/situations" class="text-indigo-600 font-semibold hover:text-indigo-500">
          Related situations →
        </a>
      </footer>
    </div>
  </article>
</BaseLayout>

<style is:global>
  .prose h2 {
    @apply text-2xl font-bold text-gray-900 mt-12 mb-4;
  }
  .prose p {
    @apply text-lg text-gray-600 leading-8 mb-6 text-pretty;
  }
  .prose ul {
    @apply list-disc list-inside mb-6 space-y-2 text-gray-600;
  }
  .prose li {
    @apply text-lg leading-8;
  }
</style>
